# HAL PHP Project - Інструкція з встановлення / Инструкция по установке

## 📦 Скачування / Скачивание

Архів з проектом доступний для скачування:
**`/api/download-project/php`**

---

## 🚀 Встановлення на хостинг / Установка на хостинг

### Крок 1: Вимоги / Требования
- PHP 7.4+ (рекомендується 8.0+)
- MySQL 5.7+ або MariaDB 10.3+
- Apache з mod_rewrite або Nginx

### Крок 2: Завантаження файлів
1. Розпакуйте архів `hal-php-project.zip`
2. Завантажте вміст папки `php-project/` в кореневу директорію вашого хостингу (зазвичай `public_html/` або `www/`)

### Крок 3: Налаштування бази даних
1. Створіть базу даних MySQL:
```sql
CREATE DATABASE hal_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. Імпортуйте схему бази даних:
```bash
mysql -u ваш_логін -p hal_db < api/database/schema.sql
```

3. Відредагуйте файл `api/config/database.php`:
```php
private $host = 'localhost';
private $db_name = 'hal_db';
private $username = 'ваш_логін';
private $password = 'ваш_пароль';
```

### Крок 4: Наповнення даними (опційно)
```bash
cd api/database
php seed.php
```

### Крок 5: Налаштування веб-сервера

#### Apache
Переконайтеся, що:
- mod_rewrite увімкнено
- AllowOverride All встановлено для вашої директорії

#### Nginx
Додайте до конфігурації:
```nginx
location /api {
    try_files $uri $uri/ /api/index.php?$query_string;
}

location / {
    try_files $uri $uri.html $uri/ =404;
}
```

---

## 📂 Структура проекту

```
/
├── api/                    # PHP Backend
│   ├── config/            # Конфігурація
│   │   ├── config.php     # Основні налаштування
│   │   └── database.php   # Підключення до БД
│   ├── controllers/       # Контролери
│   ├── database/          # Схема та seed
│   ├── helpers/           # Допоміжні класи
│   └── index.php          # Точка входу API
├── assets/                # Статичні файли
│   ├── css/style.css     # Стилі
│   └── js/app.js         # JavaScript
├── index.html            # Головна сторінка
├── search.html           # Пошук
├── company.html          # Сторінка компанії
├── blog.html             # Блог
├── contacts.html         # Контакти
├── about.html            # Про нас
├── login.html            # Вхід
├── register.html         # Реєстрація
├── dashboard.html        # Особистий кабінет
├── add-business.html     # Додати компанію
└── .htaccess             # Налаштування Apache
```

---

## 🔌 API Endpoints

### Компанії
- `GET /api/companies` - список компаній (з фільтрацією)
- `GET /api/companies/{id}` - деталі компанії
- `POST /api/companies` - створити компанію (потрібна авторизація)

### Авторизація
- `POST /api/auth/register` - реєстрація
- `POST /api/auth/login` - вхід
- `GET /api/auth/me` - поточний користувач

### Категорії
- `GET /api/categories` - список категорій

### Відгуки
- `GET /api/companies/{id}/reviews` - відгуки компанії
- `POST /api/companies/{id}/reviews` - додати відгук

### Блог
- `GET /api/blog` - статті блогу
- `GET /api/blog/{id}` - стаття

### Контакти
- `POST /api/contact` - надіслати повідомлення

---

## 🌐 Функціональність

✅ **Реалізовано:**
- Головна сторінка з каталогом компаній
- Пошук з фільтрацією по категоріях (URL змінюється)
- Сторінки компаній з галереєю до 10 зображень
- Карта розташування (OpenStreetMap)
- Двомовність (UA/RU)
- Система відгуків та рейтингів
- Реєстрація та авторизація користувачів
- Особистий кабінет зі статистикою
- Блог з статтями
- Форма зворотного зв'язку

---

## 🔐 Безпека

⚠️ Перед продакшеном:
1. Змініть `JWT_SECRET` в `api/config/config.php`
2. Встановіть безпечний пароль для бази даних
3. Вимкніть відображення помилок PHP

---

## 📞 Підтримка

Якщо виникли питання або проблеми, зверніться до технічної підтримки.
