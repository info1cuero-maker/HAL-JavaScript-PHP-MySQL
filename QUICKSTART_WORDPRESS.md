# 🚀 БЫСТРАЯ ИНСТРУКЦИЯ: Загрузка данных в WordPress

## ✅ Файлы готовы к загрузке

Все файлы находятся в папке: `/app/backend/wordpress_export/`

📦 **Файлы для загрузки:**
1. `companies_for_wordpress.csv` - 10 компаний
2. `blog_posts_for_wordpress.csv` - 2 статьи
3. `hal_wordpress_export.xml` - все данные в XML
4. `companies.json` - JSON данные компаний
5. `blog_posts.json` - JSON данные статей

---

## 🎯 РЕКОМЕНДУЕМЫЙ СПОСОБ: WP All Import (Самый простой)

### Шаг 1: Установите плагин

1. Войдите в **WordPress Admin** → `https://ваш-сайт.com/wp-admin`
2. Перейдите в **Плагины** → **Добавить новый**
3. Найдите **"WP All Import"**
4. Нажмите **"Установить"** → **"Активировать"**

### Шаг 2: Импорт компаний

1. В меню **All Import** → **New Import**

2. **Загрузите файл:**
   - Click "Upload a file"
   - Выберите `companies_for_wordpress.csv`
   - Click "Continue to Step 2"

3. **Выберите тип поста:**
   - "New Items" → выберите **"Listings"** (или создайте новый custom post type)
   - Click "Continue to Step 3"

4. **Настройте поля** (перетащите мышкой):
   ```
   CSV Column                  →  WordPress Field
   ────────────────────────────────────────────────
   post_title                  →  Title
   post_content                →  Content
   category                    →  Categories
   phone                       →  _listing_phone (Custom Field)
   email                       →  _listing_email (Custom Field)
   website                     →  _listing_website (Custom Field)
   city                        →  _listing_city (Custom Field)
   address                     →  _listing_address (Custom Field)
   image_url                   →  Featured Image (set from URL)
   rating                      →  _listing_rating (Custom Field)
   ```

5. **Settings:**
   - ✅ "Download images from URLs"
   - ✅ "Set images as Featured Image"
   - ✅ "Create categories if they don't exist"

6. Click **"Continue"** → **"Confirm & Run Import"**

### Шаг 3: Импорт статей блога

Повторите те же шаги, но:
- Загрузите `blog_posts_for_wordpress.csv`
- Выберите тип поста **"Posts"** (обычные записи)
- Настройте поля аналогично

---

## 📝 АЛЬТЕРНАТИВНЫЙ СПОСОБ: WordPress Importer

### Шаг 1: Установите импортер

1. **Tools** → **Import** → **WordPress**
2. Install "WordPress Importer" plugin

### Шаг 2: Импорт

1. **Tools** → **Import** → **WordPress** → **Run Importer**
2. Загрузите файл `hal_wordpress_export.xml`
3. Настройки:
   - ✅ Download and import file attachments
   - Assign authors (выберите пользователя)
4. Click **"Submit"**

**⚠️ Внимание:** XML метод может не импортировать custom fields корректно!

---

## 🔧 Настройка Custom Post Type (Важно!)

Если компании должны быть в custom post type "Listings":

### Вариант 1: Через плагин

1. Установите **"Custom Post Type UI"**
2. **CPT UI** → **Add/Edit Post Types**
3. Создайте:
   ```
   Post Type Slug: listing
   Label: Listings
   ```
4. В "Supports" отметьте:
   - ✅ Title
   - ✅ Editor
   - ✅ Featured Image
   - ✅ Custom Fields

### Вариант 2: Код (добавьте в functions.php)

```php
function create_listing_post_type() {
    register_post_type('listing', array(
        'labels' => array(
            'name' => 'Компанії',
            'singular_name' => 'Компанія'
        ),
        'public' => true,
        'has_archive' => true,
        'supports' => array('title', 'editor', 'thumbnail', 'custom-fields'),
        'show_in_rest' => true
    ));
}
add_action('init', 'create_listing_post_type');
```

---

## 📂 Где находятся файлы для скачивания

**На сервере:**
```
/app/backend/wordpress_export/
```

**Скачать на локальный компьютер:**

```bash
# Если у вас есть SSH доступ:
scp user@server:/app/backend/wordpress_export/*.csv ./
scp user@server:/app/backend/wordpress_export/*.xml ./

# Или через FTP/SFTP клиент (FileZilla, WinSCP)
# Подключитесь к серверу и скачайте папку wordpress_export/
```

**Или через браузер:**
Если файлы доступны через веб:
```
https://ваш-домен.com/wordpress_export/companies_for_wordpress.csv
```

---

## ✅ Проверка после импорта

### В WordPress Admin:

1. **Компании импортированы?**
   - Перейдите в **Listings** (или Posts)
   - Должно быть 10 компаний

2. **Custom Fields заполнены?**
   - Откройте любую компанию
   - Прокрутите вниз до "Custom Fields"
   - Должны быть поля: `_listing_phone`, `_listing_email`, etc.

3. **Изображения загружены?**
   - **Media** → **Library**
   - Должны быть загружены featured images

4. **Категории созданы?**
   - **Listings** → **Categories**
   - Должны быть: cafe, sport, beauty, home, construction, etc.

5. **Статьи блога импортированы?**
   - **Posts** → **All Posts**
   - Должно быть 2 статьи

---

## 🌍 Двуязычность (Опционально)

Если нужна поддержка UA/RU языков:

### Вариант 1: Polylang (Бесплатный)

1. Установите плагин **"Polylang"**
2. **Languages** → добавьте **Ukrainian** и **Russian**
3. Для каждого поста:
   - Установите язык (UK)
   - Создайте перевод (RU) используя `post_title_ru` и `post_content_ru`

### Вариант 2: WPML (Платный)

1. Купите и установите **WPML**
2. Настройте языки
3. Импортируйте украинские версии
4. Затем русские, указав их как переводы

---

## 🆘 Частые проблемы

### ❌ "Custom fields не показываются"

**Решение:**
- В редакторе поста: **Screen Options** (правый верхний угол) → ✅ "Custom Fields"

### ❌ "Изображения не загружаются"

**Решение:**
- Проверьте что URLs изображений доступны
- В WP All Import: убедитесь что выбрано "Download images from URLs"
- Увеличьте лимиты в php.ini:
  ```
  max_execution_time = 300
  memory_limit = 256M
  ```

### ❌ "Кириллица отображается как ??????"

**Решение:**
- В wp-config.php проверьте:
  ```php
  define('DB_CHARSET', 'utf8mb4');
  ```

### ❌ "Дублируются записи при повторном импорте"

**Решение:**
- В WP All Import: установите "Unique Identifier" (например, email)
- Выберите "Update existing posts instead of creating new ones"

---

## 📞 Нужна помощь?

1. **Детальная инструкция:** `/app/WORDPRESS_UPLOAD_GUIDE.md`
2. **Проблемы с custom post type:** Используйте плагин "Custom Post Type UI"
3. **Проблемы с импортом:** WP All Import имеет отличную документацию

---

## 📊 Итоговый чек-лист

- [ ] Скачал файлы из `/app/backend/wordpress_export/`
- [ ] Установил WP All Import в WordPress
- [ ] Создал custom post type "Listings" (если нужно)
- [ ] Импортировал компании из `companies_for_wordpress.csv`
- [ ] Импортировал статьи из `blog_posts_for_wordpress.csv`
- [ ] Проверил что custom fields заполнены
- [ ] Проверил что изображения загружены
- [ ] Настроил категории
- [ ] (Опционально) Настроил мультиязычность

**🎉 Готово! Данные загружены в WordPress!**
